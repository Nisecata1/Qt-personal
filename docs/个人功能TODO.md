#

# ui界面

1. 加一个关闭程序按钮
2. 2. ctrl+E快捷配置脚本映射功能：交互逻辑debug
3. alt启动脚本快捷键，显式放在ui界面并可以更改按键
   配置存在C:\develop\QtScrcpy-boost\output\x64\Release\config\userdata.ini 的 [common] 下
4. 容易误触alt然后鼠标拖动窗口导致错误发生，设定一个快捷键~让鼠标固定在电脑的投屏窗口之内
5. 配置文件C:\develop\QtScrcpy-boost\output\x64\Release\config\userdata.ini中
   [common] 下有哪一个配置没有开放在ui面板上的吗，把没开放的配置解释一下用途，并分析需不需要开放在ui上

6. 大改，左列中，在设备显示界面上方新增一行，放一个可下拉菜单，可以选中当前已连接设备，并可以手动点击切换，切换后更新该设备的全部独有板块的配置
